<h1>Ads</h1>

{{#each ads}}
  <div class="ad">
    <h2>{{this.id}}</h2>
    <p>{{title}}</p>
    <p>{{subcategory_id}}</p>
    <p>{{description}}</p>
    <p>{{cost}}</p>
    <p>{{images}}</p>
    <p>{{features}}</p>
  </div>
{{/each}}